services:
  - type: web
    name: galaxy-nema-app
    env: docker
    buildCommand: |
      mvn clean package  # Genera il JAR
      docker build -t galaxy-nema-app .  # Costruisci l'immagine Docker
    startCommand: docker run -p 8080:8080 galaxy-nema-app  # Avvia il container
